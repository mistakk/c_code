using namespace std;
#include <iostream>
#include <vector>
#include <string>

#include <algorithm>
#include <map>
#include <set>
#include <queue>
#include <unordered_set>

#include <fstream>
#include <numeric>



class Solution {
public:
	bool xorGame(vector<int>& nums) {
		return¡¡(nums.size() % 2 == 0 || std::accumulate(nums.start(), nums.end(), 0, bit_xot<int>());
	}
};

int main() {
	Solution a;
	return 0;
}